# Assets

Drop your project logo here as `logo.png`, and any custom weather/UI icons
into `icons/`. The app currently uses emoji for weather icons so it works
out of the box with zero assets required — swap in `icons/*.png` and update
`utils/helpers.describe_weather_code()` if you'd prefer custom artwork.
